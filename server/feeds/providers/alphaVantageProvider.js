export const alphaVantageProvider = { id:'alphaVantage', name:'Alpha Vantage', type:'market_data', requiresCredentials:true, supportsTicks:false, supportsCandles:true, supportsOrderBook:false, status(credentials={}){ if(!credentials.apiKey) return { status:'missing_credentials', connected:false, warnings:['Alpha Vantage API key not configured.'] }; return { status:'configured_rate_limited', connected:false, warnings:['Alpha Vantage free-tier/API rate limits apply.'] }; }, validateCredentials(credentials={}){ return { valid:Boolean(credentials.apiKey), warnings: credentials.apiKey ? ['Rate limits may throttle responses.'] : ['apiKey is required'] }; }, start(){ return {status:'not_implemented',connected:false}; }, stop(){ return {status:'stopped',connected:false}; }, getLatestTick(){ return null; }, getLatestCandle(){ return null; }, getLatestOrderBook(){ return null; } };
