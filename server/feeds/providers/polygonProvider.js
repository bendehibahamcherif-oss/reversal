export const polygonProvider = { id:'polygon', name:'Polygon', type:'market_data', requiresCredentials:true, supportsTicks:true, supportsCandles:true, supportsOrderBook:true, status(credentials={}){ if(!credentials.apiKey) return { status:'missing_credentials', connected:false, warnings:['Polygon API key not configured.'] }; return { status:'configured_not_connected', connected:false, warnings:['Credentials configured. Live websocket connector not started in this phase.'] }; }, validateCredentials(credentials={}){ return { valid:Boolean(credentials.apiKey), warnings: credentials.apiKey ? [] : ['apiKey is required']}; }, start(){ return { status:'not_implemented', connected:false}; }, stop(){ return { status:'stopped', connected:false}; }, getLatestTick(){ return null; }, getLatestCandle(){ return null; }, getLatestOrderBook(){ return null; } };
